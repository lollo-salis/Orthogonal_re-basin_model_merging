# Orthogonal_re-basin_model_merging
An exploration of orthogonal transformations as a means to align and merge independently trained neural networks, relaxing the permutation constraint of Git Re-Basin.
